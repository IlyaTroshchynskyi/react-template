# Committed defaults for Playwright E2E tests.
# Do NOT put secrets here — this file is checked into git.
# For local overrides (e.g. pointing at a real backend), copy values into .env.e2e.local instead.

USE_REAL_BACKEND=false
API_URL=http://localhost:3001
